<h1>All restaurants</h1>

<%= link_to("Add a restaurant", new_restaurant_path) %>

<ul>
  <% @restaurants.each do |restaurant| %>
    <li><%= link_to(restaurant.name, restaurant_path(restaurant)) %> <%= "⭐️" * restaurant.rating %></li>
  <% end %>
</ul>
